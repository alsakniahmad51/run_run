import 'package:flutter/material.dart';

class MyWidgetaaa extends StatelessWidget {
  const MyWidgetaaa({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("vknadkvnak"),
    );
  }
}
